'use client';

/**
 * AdsterraScript component
 * This component has been modified to return null as Adsterra has been removed.
 * Kept for backward compatibility to prevent import errors.
 */
const AdsterraScript = () => {
  return null;
};

export default AdsterraScript;
